node_modules
.next
out
.env*
!.env.example
.DS_Store

.vercel
.env*
tsconfig.tsbuildinfo

# Session-local Claude Code config (broad Bash-allow, machine-specific)
.claude/settings.json

# Throwaway one-off verification scripts (see CLAUDE.md's testing convention)
scratch-*.mjs
/scripts_scratch/
