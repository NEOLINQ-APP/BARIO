# BARIO — Working TODO

Add items anywhere, any time. Claude checks this file and updates it as work gets done — check items off with `[x]` yourself if you handle something outside a session too.

## Blocked — needs something from you
- [ ] `SENTRY_API_TOKEN` — needed so the admin assistant's error feed actually works (currently shows "not connected").
- [ ] Switch `afclogistics.ca`'s nameservers at Hostinger (its registrar) to `aron.ns.cloudflare.com`/`renan.ns.cloudflare.com` — needed to finish that migration.
- [ ] Switch `rapturemedia.ca`'s nameservers at Bluehost (its registrar) to the same Cloudflare pair — needed to finish that migration.
- [ ] `afc-backend`'s SMTP isn't configured (`.env` has `SMTP_HOST`/`SMTP_USER`/`SMTP_PASS`/`SMTP_FROM` all commented out) — needed for password-reset emails AND for auto-emailing approved drivers their login (currently falls back to showing the admin the password to send manually). Give me real SMTP credentials (Hostinger email for afclogistics.ca, or any provider) whenever you want this turned on.
- [ ] RunPod video generation — 2 real bugs found+fixed 2026-08-01 (see Done below), commit pushed to trigger a rebuild, but never got final confirmation the new image actually picked up the fix. Needs a live re-test.
- [ ] One real Hostinger mailbox's actual login (any of the 6 clients) — needed to verify the SOGo auxiliary-IMAP trick end-to-end (does reply-sending work correctly as the real address, not just receiving).

## In progress
- [ ] **Bario One — modular, pay-per-module repackaging (raised 2026-08-11), full plan at `.claude\plans\unified-wishing-salamander.md`.** Goal: sell Bario One a-la-carte (CRM/Invoicing/Payments/Employees/Payroll/POS/AI Assistant/API-Integrations as independently-priced modules) instead of fixed Starter/Professional/Business tiers, so a business only pays for what it wants and can add more later — directly competing with Monday/Twenty on price/flexibility while keeping the cross-module story (CRM+Invoicing+Payments+Payroll+POS+AI in one login) neither of them offers.
  1. [x] **Phase 1 (schema + gating) — DONE 2026-08-11, live-verified.** `bo_organizations.enabled_modules_json` is now the real entitlement source (the old `plan` column stays display-only); `requireBoModule(key)` swapped into every one of the ~65 Bario One route files; the Flo API's separate Bearer-key auth path gated the same way. **Real bug caught live**: the first backfill design derived a pre-existing org's modules from the old marketing-tier copy, which silently took Payroll+POS away from a real org actively using both (that org's `plan` was "professional," a tier whose old copy never listed either even though nothing was ever actually restricted) — fixed to backfill ALL modules for any pre-existing org, since that's the real historical truth. New admin route `/api/admin/bario-one/organizations/[id]/modules` for support-side overrides. Seat limits now a formula (2 + 3/module, unlimited past 5) instead of a fixed per-tier constant.
  2. [x] **Phase 2 — DONE 2026-08-11, live-verified.** One recurring Stripe Price per module (`lib/stripe.ts`'s `BO_MODULE_PRICE_IDS`), `modules/checkout` (first activation, multi-item Checkout) + `modules/update` (self-serve add/remove via `subscriptionItems.create`/`del`) routes, webhook reconciliation so Stripe's real subscription items — not just `subscription_status` — drive `enabled_modules_json` going forward. Verified the Stripe-specific mechanics in TEST MODE first via a throwaway script (real test Products/Prices, a real multi-item Checkout Session, a real test subscription created directly via `pm_card_visa`, the reverse price-lookup checked against that real subscription, an add + a remove matching the update route's exact calls) before deploying anything. **Real live module Stripe Prices don't exist yet on purpose** — the admin route to create them (`/api/admin/bario-one/setup-module-prices`) is built and ready, but that's a real-money "go live" step needing an explicit go-ahead, not something to do silently.
  3. [ ] **Phase 3 (not started)** — dashboard UI: locked/unlocked module tiles, self-serve `/dashboard/bario-one/modules` page.
  4. [ ] **Phase 4 (not started)** — public pricing/signup page rewrite (module picker replacing the 3-tier grid), de-duplicate the two existing signup forms, remove the stale all-"Coming soon" module list on the marketing page.
  Placeholder pricing (needs real sign-off before going live, same open item already tracked for VPS/domain pricing): CRM $29, Invoicing $19, Payments $9, Employees $19, Payroll $39, POS $29, AI Assistant $19, API & Integrations $19 (all /mo CAD).
- [x] **Bario One CRM — close the gap with Monday.com/Twenty CRM (raised 2026-08-08) — ALL 3 PHASES DONE, live-verified 2026-08-08.** Custom fields, multiple pipelines + view toggle, and a no-code automation builder — see below for full detail on each. Lower-priority items intentionally deferred: reporting dashboard, CSV import, two-way email/inbox sync, granular permissions, comments/@mentions. User wants Bario One's CRM ("our own") brought up to feature parity with Monday.com and Twenty CRM. Compared our actual schema/routes (`lib/db.ts` `bo_customers`/`bo_deals`/`bo_tasks`/`bo_notes`, `app/api/bario-one/crm/*`) against both products' current real feature sets (checked via WebSearch, not recalled) rather than guessing. **Confirmed real parity already**: contacts, one deal pipeline w/ stages, tasks, notes/email/SMS history, a working tool-calling AI assistant (Bario AI), our own API + outbound webhooks (just shipped, see [[bario_one_platform]]), and a genuine edge neither competitor has — Invoicing/Payments/Payroll/POS bundled in the same product (they're CRM-only). **Confirmed real gaps**: no custom fields at all (fixed columns only), one hardcoded pipeline/stage list (no custom objects, no multiple pipelines), only one view (no kanban/calendar/timeline/list toggle), no in-app automation builder (webhooks exist as the action-layer plumbing but nothing no-code sits on top), no reporting/dashboards beyond ad-hoc AI queries, only owner/admin/employee roles (no field/board-level permissions), CSV export only (no import), one-way email send only (no inbox/reply tracking), plain notes only (no @mentions/comments).
  **Agreed build order** (each phase deliberately depends on the last):
  1. [x] **Custom fields — DONE 2026-08-08, live-verified.** Per-org field definitions (name/type/options: text/number/date/select/checkbox) attachable to customers and/or deals (`bo_custom_fields` table, values stored as a JSON map on `bo_customers.custom_fields_json`/`bo_deals.custom_fields_json`, keyed by field id). New `/dashboard/bario-one/crm/fields` management page (owner/admin only), wired into customer detail + deal cards/creation. `lib/barioOneCustomFields.ts`'s `mergeCustomFieldValues` silently drops any field id that isn't a real definition, so a deleted field def can never corrupt other rows. Verified: value persists correctly for both entity types, unknown field id ignored not stored, cross-tenant 404, delete works, all touched pages render clean.
  2. [x] **Multiple pipelines + view toggle — DONE 2026-08-08, live-verified.** `bo_pipelines`/`bo_pipeline_stages` tables; `bo_deals.stage` stays a plain TEXT key for backward compat (existing lead/opportunity/quote/won/lost values untouched), stages just add structure/custom-naming on top. Every org lazily gets a "Sales Pipeline" default (exact legacy 5 stages) the first time it's touched, via `ensureDefaultPipeline` in `lib/barioOnePipelines.ts` — also backfills any pre-existing deals onto it, no migration script/downtime. Stage validation is now pipeline-scoped (a deal can only take a stage key from its own pipeline, checked server-side). Deletion guarded 4 ways: can't delete the default pipeline, can't delete a pipeline with deals in it, can't delete a stage with deals in it, can't leave a pipeline with zero stages. UI: pipeline selector + "+ New pipeline", stage manager (add/remove), and a Kanban/List/Calendar view toggle in `components/BarioOneCrmPipeline.tsx` — calendar is a real month grid (prev/next nav) grouping deals by `expected_close_date`, with an "Unscheduled" bucket for deals with none.
  3. [x] **No-code automation builder — DONE 2026-08-08, live-verified.** `bo_automations`/`bo_automation_runs` tables (`lib/barioOneAutomations.ts`). Trigger vocabulary: deal.created, deal.stage_changed (optionally filtered to a specific pipeline+stage), customer.created, invoice.paid — deal events are new, wired into the deal create/PATCH routes (stage-change detection compares old vs new stage server-side, only fires on a real transition). Actions deliberately limited to safe/additive ops only (create_task, add_tag, add_note, send_email, send_sms) — same tool-list-as-security-boundary pattern as the admin/Bario AI assistants, no delete/refund/mark-paid action exists to configure. Config validated at creation time per action type. Execution is non-blocking/never-throws (same contract as `triggerWebhooks`), logged to `bo_automation_runs` for a visible per-automation audit trail. UI at `/dashboard/bario-one/crm/automations`: trigger+action builder form, pause/resume toggle, run log. Verified live: a "deal moves to Won → create task" rule and a "customer created → add tag" rule both fired with real side effects, confirmed the stage filter doesn't fire on a non-matching stage move, confirmed pause stops execution and resume restores it, confirmed config validation rejects incomplete input, confirmed cross-tenant 404.
  **All 3 phases of this plan are now complete.** Lower-priority, not yet scheduled: basic reporting dashboard, CSV import, two-way email/inbox sync, granular permissions, comments/@mentions.
- [ ] **Bario Studio Ecosystem — full editor+copilot+design+print, shipped and live-verified 2026-07-31; only the AI-generation piece is blocked on RunPod.** Started as a simple generate-and-preview AI video/voiceover flow (self-hosted Wan 2.2 + Kokoro on RunPod — see below for why, not fal.ai/ElevenLabs/OpenCut). User then explicitly asked to pause that and expand it into a real CapCut/Canva-style ecosystem: multi-track editor, AI copilot chat, static design templates, print export, all monetized. A pasted brief describing a hand-built Rust/WASM compositor + GPUI desktop app was researched and **rejected** — that's literally redoing OpenCut's own multi-year unfinished rewrite. Instead: adopted `@openvideo/core` + `@openvideo/timeline` + `@openvideo/engine-pixi` — real, MIT-licensed, published npm packages (confirmed via `npm view`) backing the open-source `openvideodev/react-video-editor` project (1.8k stars) — installed directly into Bario's existing Next 14 app (no Next/Tailwind version upgrade needed, since these are framework-agnostic libraries, not a fork of their demo app).
  **Shipped, each one live-tested with a real disposable account + Playwright, screenshots taken:**
  1. Core engine proof: upload/trim/export a real MP4 via WebCodecs (`Compositor`) — one real bug found+fixed (`Video.fromUrl()` needed for export, not raw clip data).
  2. AI generation (video+voiceover, with a real Kokoro voice picker — 6 verified real voice IDs, male/female/US/UK) wired in as a canvas asset source, replacing the old standalone preview page. `components/StudioEditor.tsx`.
  3. Text overlays + user-uploaded music tracks. **Known limitation, not silently papered over**: Text clips don't currently export into the MP4 (a real `@openvideo/engine-pixi` renderer-wiring issue, two fix attempts didn't resolve it) — text still shows correctly in the live preview, export just skips it with a clear on-screen note.
  4. AI copilot chat ("just like Zeus") — `/api/studio/copilot`, tool-calling (generate_video, generate_voiceover, add_text_overlay), any language. Architecturally different from Zeus: tools are decided server-side but *executed client-side* against the browser-only `@openvideo/core` state. Respects the AUP gate rather than auto-accepting it for the user. Hit and fixed a real OpenAI 400 (`gpt-5.6-luna` needs `reasoning_effort:'none'` for tool calls on the chat-completions endpoint).
  5. Static design mode (`components/StudioDesignEditor.tsx`) — Instagram/Facebook/X post+story templates, same engine, PNG export via `Studio.snapshot()`. Hit and fixed a real, serious bug: recreating the PixiJS engine on a *reused* `<canvas>` DOM node when switching templates froze the entire browser tab (even a screenshot request hung) — fixed by keying the canvas component so React fully remounts a fresh canvas per template.
  6. Copilot extended to design mode: `add_text_overlay` + a new `search_stock_image` tool (reuses Zeus's existing Unsplash integration, not a second image-search build).
  7. Print templates (business card 3.5×2in, flyer/brochure Letter, yard sign 18×24in) with real physical dimensions, 0.125in bleed baked into canvas size, and genuine print-ready PDF export (`pdf-lib`) — verified by fetching the exported blob and checking the real `%PDF-` file header. Sign uses 100 DPI, not 300 — real large-format print practice, not a corner cut. Honest caveat shown in-UI: exports high-res RGB, not a full CMYK color-managed file.
  8. Royalty-free music library: deliberately **not** built — this is a real ongoing vendor cost + licensing commitment, flagged back to the user with concrete options (Mubert API for AI-generated tracks; Soundstripe/Epidemic Sound as curated subscription libraries, embeddability unconfirmed) rather than auto-picked.
  **Still blocked on the user**: the actual AI generation (video/voiceover) needs a real RunPod account — see `studio-worker/README.md`. Everything else above (editor, copilot's non-generation tools, design mode, print export) works today independent of that.
- [ ] **Zeus site-generation streaming + Claude Sonnet 5 comparison (2026-07-30)** — shipped and live: fixed a real schema bug (open `z.record()` for section data silently produced empty sections under Claude's strict tool mode while the explanation text still claimed success — replaced with a closed discriminated union, one schema per section type), added a direct `@anthropic-ai/sdk` generation path (bypasses the Vercel AI SDK's `generateObject`, which fails outright — "compiled grammar is too large" — on this schema's size under Claude's strict mode), and converted the whole route to NDJSON streaming so the builder canvas shows a live "Zeus is building..." preview with fade/slide-in sections instead of one blocking response. Idle-abort timer raised from a fixed 55s to a 45s-since-last-chunk timer (was firing on cold-start connection delay, not real stalls).
  **UPDATE 2026-07-30, later same day — this was resolved, no longer an open decision:** switched Zeus over to OpenAI's newly-released GPT-5.6 Luna (`gpt-5.6-luna` — OpenAI's new cost/speed tier, 1.05M-token context, released July 9 2026; note the bare `gpt-5.6` alias routes to the flagship "Sol" tier, not Luna — the exact suffixed model ID is required). Swapped the OpenAI code path's model string (was `gpt-4o-mini`), raised the pre-flight token-budget guard from 100k to 800k to match Luna's much larger context, and **flipped `ZEUS_MODEL_PROVIDER` in Vercel production from `anthropic` to `openai`** so this is now the live, active model — not a dormant option. Verified live end-to-end with a real disposable-account generation call: clean `done` event, real multi-section site (hero/features/contact/cta/footer/nav/pagelinks), a genuine Unsplash image URL, no errors, **~24s total** — meaningfully faster than Claude Sonnet 5's 45-65s and worth rechecking against gpt-4o-mini's old few-second baseline if speed regresses further. If quality complaints come up (Claude's stronger multi-page structuring was the reason it was tried in the first place), that's the tradeoff being made here — Luna hasn't been A/B tested against Claude on structuring quality yet, only confirmed to produce valid working output.
- [ ] **Zeus vs. Lovable feature-parity discussion (2026-07-30), not yet started.** Asked what Lovable has that Zeus doesn't; answered with 5 items (confirmed live via WebSearch, not just recalled): GitHub sync/real code export, version history + rollback, Plan Mode (shows a plan before generating, user approves), Prompt Queue (stack up to 50 instructions), automated post-edit browser testing. Follow-up: user asked why Zeus doesn't have these too. Answer: **4 of the 5 are not architecturally blocked at all** — version history (a `site_versions` snapshot table + restore endpoint, same shape as the existing `admin_actions_log`), Plan Mode (a cheap preliminary call showing a bullet-point plan + Approve/Adjust before the real generation runs), Prompt Queue (removing the current `if (busy) return` block on new messages and queuing instead), and a GitHub-export version of the existing Export-HTML button — all buildable on Zeus's current section-JSON architecture, just never prioritized. Only automated browser testing needs real new infrastructure (a headless browser after each edit — not a fit for a Vercel serverless function the way the others are). Was about to ask user to scope (build all 3 easy ones now, or version history first) when the session was paused to checkpoint here instead — **pick this back up by asking that question.**
- [ ] **"Design Engine" — 7 named style presets for Zeus, not started (raised 2026-07-29/30).** User wants: Lovable Modern SaaS, Stripe Enterprise, Linear Dark, Apple Minimal, Framer Startup, Luxury Agency, Construction Company Premium — alongside or replacing the current 5 presets (modern/elegant/bold/minimal/friendly) in `lib/stylePresets.ts`. **Real prerequisite found, not yet done:** `lib/renderSite.ts`'s `EXPORT_CSS` hardcodes page background/text colors directly (`#f8faff`, `#64748b`, `#f0f4ff`, etc.) instead of via CSS custom properties, so a genuinely dark preset ("Linear Dark") isn't achievable until those are refactored to themeable vars first — contained but real work, do that before/alongside adding the 7 presets. Do NOT remove the existing 5 preset keys when adding these — live customer sites have `theme.style` values saved against them (`elegant`/`bold`/etc.); removing them would silently break those sites' rendering.
- [x] **CRM lead-gen automation — shipped 2026-07-30, first piece of a much bigger ask.** User's actual ask was a full AI agent per CRM handling leads, email (read/send/marketing), social media (posts/followers/DM replies), ads, phone calls, and contact-history Q&A. Checked what's real before building: **social media/ads are blocked** — `lib/marketing/*` already has the posting code but zero platform credentials are configured in production (no `TWITTER_API_KEY`/`META_APP_ID`/`LINKEDIN_CLIENT_ID`/`GOOGLE_BUSINESS_CLIENT_ID`) and that system is a single shared BARIO-owned connection anyway, not per-client — would need a real redesign to support two separate businesses' own accounts. **Phone calls are entirely unbuilt** — no Twilio account exists. **What shipped:** `/api/cron/crm-leadgen` (new, runs every 6h alongside vps-reconcile) polls both Twenty CRM instances for People, drafts a genuinely personalized outreach email per contact via GPT-5.6 Luna, and logs it as a Note on their record — verified live, real quality output, idempotent (tracked in new `crm_leadgen_drafted` table, won't redraft the same person twice). **Deliberately does NOT send** — sending "as" afclogistics.ca or sunbuiltgroup.com needs a verified sending domain/mailbox for each business (their real email still lives on their original hosts, untouched during the BARIO DNS cutover), which doesn't exist yet. Built via long-lived Twenty CRM API keys (`AFC_CRM_API_KEY`/`SUNBUILT_CRM_API_KEY` in Vercel, also in [[bario_credentials_reference]]) rather than the originally-discussed n8n-workflow route — n8n (`flo-automations-n8n-1`, `https://automations.crm.bario.ca`) is running but has no known login, and a hand-written BARIO API route is more reliable/debuggable than blindly automating its UI. **Not started yet:** the contact-history summarization/Q&A assistant (same pattern as BARIO's own admin assistant, pointed at Twenty's API instead) — a separate, smaller piece from lead-gen, not touched this round.
  **Update, same day:** both CRMs originally only had Twenty's stock demo data (Ivan Zhao/Notion, Dario Amodei/Anthropic, etc. — not real prospects), so the first lead-gen run was drafting outreach to fake companies. Deleted all demo people/companies from both workspaces and replaced with 6 genuinely real leads found via web search, using only publicly-listed contact info from each company's own site (no scraping, no fabricated names/emails): **AFC Logistics** — Western Oilfield Specialties Corp, Excell Metal Fabrication, Advantage Welding and Fabrication Ltd; **Sunbuilt Group** — Braden Equities Inc, YEG Xpanded, OPUS Corporation. Re-ran the cron — all 6 got real, well-targeted drafted outreach.
  **Update, same day again — scaled up to ~100 each per request.** User asked for "like 100 each." Individually researching 200 leads one company-site-visit at a time (like the first 6) wasn't practical, so switched to real business directory sites (cossd.com, alberta-local.ca) that list many genuine businesses per page — pulled name/phone/website (rarely email) for dozens of real Edmonton/Calgary/Alberta businesses per category, deduped, and batch-created them via a throttled script (Twenty's API rate-limits at 100 requests/60s). **Final counts: AFC Logistics 125 real companies/contacts** (oilfield equipment suppliers, machine shops, plastics/tank/electrical manufacturers — all real, plausible freight-shipping customers), **Sunbuilt Group 103 real companies/contacts** (hotels/motels, condo and property management companies across Edmonton/Calgary/Red Deer/Lethbridge/Medicine Hat/Fort McMurray — all real, plausible renovation clients). Fixed a real bug found in the process: the cron's `people(first: 50)` query meant it only ever saw the *same* first 50 contacts and would silently never reach anyone past that — changed to `first: 500` plus a `MAX_NEW_PER_RUN = 20` cap per invocation (so it still fits `maxDuration`) — called the endpoint repeatedly (8 times) to work through the full backlog. All 125 + 103 now have a real, personalized drafted outreach Note — verified quality by reading a fresh sample, still genuinely specific per business, not generic. **Still a one-time manual seed, not an ongoing pipeline** — see the "real B2B lead-data API" option above if continuous automated lead-sourcing at scale is wanted next.
  **Update, 2026-07-31 — native in-CRM AI chat ("Miko") shipped for both CRMs.** User wanted to actually chat with an AI *inside* the CRM itself, plus have it help with email drafting/replies and (eventually) social media. Investigated Twenty CRM's own compiled source first rather than bolting on something separate — it turns out Twenty has a **full native AI Agent chat system already built in** (agent chat threads, streaming, per-workspace model config), just not wired up. Got a real Anthropic API key from the user and:
  1. Added `ANTHROPIC_API_KEY` to both stacks' `.env`/`docker-compose.yml`.
  2. **Found and fixed a real bug**: both containers were only running `node dist/main` (the API server) — there was no background **worker** process (`node dist/queue-worker/queue-worker`) at all, so chat messages got accepted (HTTP 200) but silently sat in the queue forever with no response, since AI generation runs as an async job. Added a `worker` service to both docker-compose files (same image, worker command, `DISABLE_DB_MIGRATIONS`/`DISABLE_CRON_JOBS_REGISTRATION` set so it doesn't race the main container on startup).
  3. Enabled `anthropic/claude-sonnet-5` via `updateWorkspace(enabledAiModelIds:...)` (composite `provider/model-name` ID format required — bare `claude-sonnet-5` fails with "model not available in this workspace") — this call needs a real user session token, not an API key (`"API keys are not supported"` on `updateWorkspace`).
  4. Set the assistant's persona/name via `aiAdditionalInstructions` on the workspace (simplest path — this is what actually drives the built-in "New chat" panel's identity) **and** created a matching custom Agent object (`createOneAgent`) named `miko` for each workspace, in case a dedicated per-agent chat surface gets used later. `roleId` must be omitted when creating an Agent — Twenty's own "Admin" role explicitly cannot be assigned to agents.
  5. Verified live in a real browser for both: Miko correctly introduces itself as "Miko, your AI assistant here at AFC Logistics" / "...at Sunbuilt Group", lists real capabilities, and is genuinely context-aware (referenced the actual CRM view the tester was on).
  **Honest scope check on the rest of the ask, unchanged from earlier:** Miko can *draft* outreach/reply emails in chat when asked, but can't send them — same sending-domain gap as the lead-gen cron. Social media (connect accounts, post, reply to DMs) is **not a Twenty CRM concept at all** — no code path exists for it in Twenty; that would have to stay a BARIO-side feature the CRM could at best call out to once platform OAuth credentials exist, not something native to Twenty's own settings pages. Also noticed (not fixed, not recurring, harmless): one stale background job threw `LogicFunctionException: Source file not found` once during the initial worker backlog catch-up — looked like leftover seed-data cruft referencing a workflow code step with no source file; didn't reappear, not investigated further.
- [x] **`send.bario.ca` Resend verification + `EMAIL_FROM` fix — DONE 2026-07-30.** Domain flipped to `verified`. Set `EMAIL_FROM=Bario <noreply@send.bario.ca>` in Vercel prod, redeployed, and confirmed live: signed up a disposable account with an email on Bario's own Mailcow test mailbox, fetched the actual message via IMAP — real verification email arrived (`From: Bario <noreply@send.bario.ca>`, `Subject: Verify your Bario email`). This was the exact cause of the "Jasmine couldn't verify her email" bug and almost certainly affected every real customer since — see [[bario_email_delivery_broken]] equivalent note below.
- [ ] **Follow-up from the EMAIL_FROM fix, not started:** audit `users` for accounts with `email_verified = false` that signed up before 2026-07-30 — they may be silently stuck locked out of builder access the same way Jasmine was, and the old broken email meant they never got a working verification link. Decide: bulk-verify, or re-send them a (now-working) verification email.
- [x] **Mailcow installed and running on the reseller mail VPS (2026-07-29)** (`148.230.94.192` / `reseller.bario.ca`, key `~/.ssh/bario_mail_vps`) — Docker + Docker Compose installed, Mailcow cloned and configured (hostname `reseller.bario.ca`, timezone America/Edmonton), all 18 containers up (Postfix, Dovecot, Rspamd, SOGo, MySQL, ClamAV, etc.), real Let's Encrypt certificate auto-obtained (not self-signed), default `admin`/`moohoo` login changed immediately since the panel is internet-reachable. Forward DNS and reverse DNS (PTR) both confirmed correctly matching. **This is infrastructure only — no customer-facing product yet**, same as VPS's Phase 1: no mail domains added, no DKIM/SPF/DMARC configured for any real domain, no BARIO account integration, no billing, no white-labeling of the SOGo webmail UI. Next real steps: add a test domain and verify actual send/receive works, then decide the BARIO integration shape (self-serve like VPS, or admin-provisioned) before this is sellable.
- [ ] Migrate `afclogistics.ca` (marketing frontend to BARIO) — CORS + `api.afclogistics.ca` nginx vhost live on the VPS. Frontend content prep/import to BARIO not done yet. Blocked on the nameserver switch above. **Important:** the current `afclogistics.ca` file being migrated is NOT just a marketing page — see the correction below.
- [ ] Finish `rapturemedia.ca` — content imported to its own BARIO site, Cloudflare zone now created and domain connected (2026-07-27, `verified: true` from Vercel). Blocked on the nameserver switch above. Still owe: "backup"/"download" folders in Jasmine's Media Library account, not done yet (unrelated to this domain, just noted here since it was raised in the same request).

## Done
- [x] **Main VPS cleanup — real dead infrastructure found+removed (2026-08-02).** While investigating where to run a Namecheap-proxy for the domain-reseller feature, found a whole undocumented layer on `2.25.139.207`: a separate Express backend (`bario-backend`, its own SQLite DB with mostly-empty `spott_*`/`users`/`sites`/`coupons` tables — 2 users total, last write 2026-07-14), plus `bario-hub-client`/`bario-hub-server`, backing 9 nginx vhosts nobody remembered (`editor`/`creator`/`preview`/`edits`/`code-backup`/`hub`/`order`/`studio`.bario.ca, plus separately `code.bario.ca`). Checked before touching anything: DB had near-zero real activity, and most of these domains' DNS had already been repointed to Vercel/elsewhere, confirming they were already orphaned, not live. **Removed**: all 9 nginx vhosts, the 3 dead pm2 processes, and the `code-server@root.service` systemd unit (the mobile-dev-panel experiment, see above). Files left on disk (not `rm -rf`'d) in case anything's still wanted. `afc-backend`, both CRM stacks, Mailcow, and n8n were all confirmed untouched.
- [x] **Bario Dialer — shipped and live-verified, real bug fixed (2026-08-01/02).** Installable per-business softphone PWA for AFC/Sunbuilt/Unique Group. Found and fixed a real production bug via live browser testing: Voice Access Tokens were being signed with the Account SID standing in for an API Key SID, which Twilio's servers reject (`AccessTokenInvalid`) despite it looking like a valid JWT — fixed with a real Twilio API Key/Secret pair. Also shipped: internal app-to-app calling between the 3 apps (no phone number, no carrier cost), incoming-call accept/decline, CRM add-contact from the dialer, a full theme/layout redesign matching bario.ca's own dark palette, and a real layout bug fix (10-digit numbers were wrapping and pushing buttons off-screen). Full writeup in memory (`bario_dialer`).
- [x] **Email hosting — customer-facing signup shipped (2026-08-02).** New `/dashboard/email` (sidebar item), lets paid-plan customers create real mailboxes on their verified custom domain via Bario's own Mailcow server, auto-provisioning MX/SPF/DKIM DNS. Also discovered a real, ToS-clean way to bring the 6 Hostinger-hosted clients' email into Bario's own branded webmail login (SOGo's built-in auxiliary-IMAP-account feature) instead of the dead-end idea of iframing/proxying Hostinger's own webmail (blocked by `X-Frame-Options` + Cloudflare bot-protection, confirmed via live testing).
- [x] **Sky renamed (2026-08-02)** — the site-builder AI (previously "Zeus", then briefly "Bario AI") now has an actual given name, **Sky**, everywhere in UI copy and its own system prompts. Also raised the copywriting bar in its prompts (real vocabulary, no generic filler) and gave Bario Build's own AI (Miko) a document-generation capability bump (knows to use `pypdf`/`python-docx`/`openpyxl`/`python-pptx` for PDF/Word/Excel/PowerPoint output).
- [x] **RunPod unblocked (2026-08-01)** — real account/API key working, 2 production bugs found+fixed (a broken duplicate endpoint deleted, response-parsing double-nesting bug fixed in `lib/runpod.ts`, a workflow-node iteration bug fixed in `studio-worker/handler.py`). Voiceover generation verified working end-to-end live. Video generation rebuild confirmation still outstanding — see Blocked above.
- [x] **sunbuiltgroup.com content/branding pass (2026-07-30)** — three separate asks, all shipped and live:
  - **New "Gallery" page**: 29 real WhatsApp project photos (from `Downloads\Projects\SunBuiltGroup-com\`) uploaded to X-Drive, grouped into 18 named projects (e.g. "150 Olde Bayview Residence" — a real address visible in one photo — plus invented-but-tasteful cover names like "The Courtyard Stone Retreat," "Blue Agate Feature Bath" for the rest, since no other identifying info was available) across two sections (Custom Builds & Exteriors / Interior Finishes). Added as a new `page-section` + nav link inside the site's existing single-page-app raw HTML (this site uses one `content_mode:'template'` blob with JS `showPage()` section-switching, not BARIO's separate multi-page `site_pages` system). **Not yet done:** user said more photos are coming — next batch needs the same treatment (upload, group, add to the page).
  - **Services page reorganization**: found the real bug behind "feels too long and unorganized" — the 3 category sections (`svc-category`) were themselves being rendered as columns of a 3-column CSS grid (the container div had the `svc-grid` class applied directly), so categories sat side-by-side as uneven-height columns instead of stacking as proper full-width sections. Fixed by removing that class. Also replaced each of the 21 services' bulky image+5-bullet-point cards with a compact icon+name+one-line-blurb card (existing "How It Works" modal already shows the full point/timeline/includes detail, so nothing was lost) — cuts the page height roughly in half and fits 4 per row instead of 3.
  - **New logo + favicon**: user supplied a full 3D-mockup-style logo image (`Downloads\luxury_sun_crown_logo.webp`, a "SUNBUILT GROUP" wordmark under a sunburst+building emblem, on a dark photo-scene background). Used `sharp` (installed in scratch dir) to: crop the emblem alone into a 512×512 favicon, crop the "SUNBUILT GROUP" wordmark alone, and composite icon+wordmark side-by-side into a proper horizontal header-logo lockup (the source was stacked vertically, which doesn't work at a 48px-tall nav-bar height) — verified at real display size, the black-background seam between the composited pieces is imperceptible. Swapped in everywhere the old logo appeared (header, hero badge, admin login screen, footer, favicon, apple-touch-icon) and used the original full mockup image for `og:image`/`twitter:image` social-share previews.
- [x] Admin AI assistant at `/admin/assistant` (2026-07-27) — general-purpose Q&A, autonomous tool-calling for low-risk fixes (grant plan/storage, verify email, reset password, restore/publish a site, connect a domain), full audit log (`admin_actions_log`), prompt-injection guardrails, refund/cancel deliberately NOT wired as tools. Complaints intake built (`/api/support/contact`). File upload added to the customer support assistant for billing disputes. In-panel feed built; email + SMS notification channels still blocked (see above).
- [x] Spot-checked the 10 most recent signups (2026-07-27) — all verified, none stuck by the Resend bug.
- [x] Investigated `aryanarkcollection.com` — real Node.js e-commerce-style app on the VPS, not something you'd asked to migrate, documented only.
  - [x] **2026-08-05: added the bario.ca logo (small dark badge, since the icon PNG has a solid black background, not transparent) + "Hosted by bario.ca" link to its maintenance page** (`/var/www/aryanarkcollection.com/server.js`'s `MAINTENANCE_PAGE` template, backed up first as `server.js.bak-<epoch>`).
  - [x] **2026-08-05: unlocked the site** — `aryanark.collection@gmail.com` now has a real bario.ca account, so both `MAINTENANCE_MODE` and `ADMIN_LOCKED` (the "2 week trial expired" gate) were flipped to `false` in its `.env` (backed up first) and the pm2 process restarted with `--update-env`. Storefront confirmed live, admin login (`Allah1`, unchanged) confirmed unlocked. **Not done**: their bario.ca account itself still has `plan: null` — didn't invent a plan/tier grant without knowing what was actually agreed; ask Sherwin which plan if one's owed.
- [x] Added `POST /api/admin/users/create-site` after learning the hard way it was needed (see the `rapturemedia.ca`/`sunbuiltgroup.com` mixup, already resolved — `sunbuiltgroup.com`'s content was restored correctly).
- [x] X-Drive repricing + rebrand shipped and verified live (2026-07-27).
- [x] Removed the file-type restriction on X-Drive uploads (2026-07-27).
- [x] **`sunbuiltgroup.com` DNS cutover complete (2026-07-27)** — old VPS A records removed, `A @ → 76.76.21.21` + `CNAME www → cname.vercel-dns.com` live, all Hostinger email records (MX/DKIM/autoconfig/SPF/DMARC) untouched. Vercel's own verification check may take a bit to catch up but the DNS itself is correct and confirmed.
- [x] Got Cloudflare write access (2026-07-27) — token upgraded, unblocking everything above that used to say "blocked on Cloudflare token."
- [x] Dropped the TITANS Agency platform entirely per your instruction (2026-07-27) — not tracked here anymore, don't bring it back up.
- [x] **Correction (2026-07-27): `afclogistics.ca`'s single index.html is a full single-page app, not just marketing.** I only read its `<head>` early in this session and wrongly concluded "no admin/driver/customer UI exists" — built a redundant separate admin panel at `admin.afclogistics.ca` as a result. The real file already has working `page-admin`/`page-driver`/`page-customer`/`page-tracking` sections, all wired to the same real backend (JWT auth, `/api/*`). **My separate admin panel has been decommissioned** (nginx vhost + files removed) — the real one, live at `afclogistics.ca` → Admin (footer link, passcode + staff login gated), is the one that matters. See [[afc_logistics_admin_panel]] memory for the full correction and what's actually real vs. placeholder in that file.
- [x] **Fixed the 4 dead "Quick Actions" buttons (2026-07-27)** — removed 3 that duplicated existing real functionality (Assign Job Manually, Manage Users, View All Invoices), repurposed "Edit Rate Sheets" to open a new modal for setting each driver's standing $/% pay rate.
- [x] **Driver pay $/% + profit added to the REAL admin panel (2026-07-27)** — `users.default_pay_type`/`default_pay_value` (standing rate, editable via the new Rate Sheets modal), `jobs.driver_pay_type`/`driver_pay_value` (per-job, editable in Create Job). Auto-applied when a driver is assigned OR selected at job creation; overridable; recomputes automatically if price changes. Added Driver Pay + Profit columns to the Jobs table, and Driver Pay MTD / Profit MTD stat cards to the dashboard. Verified end-to-end via direct API calls (70% of $100 → $70 auto-applied, flat override, price-change recompute, and auto-apply-at-creation all confirmed with exact expected numbers).
- [x] **Verified the driver portal was already fully built and working (2026-07-27)** — asked to "work on it," but found (after reading the actual `renderDriverPortal()` function, not just placeholder HTML) it already does everything: accept/decline, pickup/in-transit/delivered status progression, POD photo upload, and auto-starting live GPS tracking the moment a driver has an active job. Verified the entire lifecycle end-to-end via direct API calls — nothing needed building.
- [x] **Added "Change My Password" to the real admin panel (2026-07-27)** — self-service password change for whoever's logged in, using the backend's existing (previously unused) `/api/auth/change-password`. Verified via API test.
- [x] **Fixed stale-cache confusion (2026-07-27)** — `afclogistics.ca`'s nginx never sent a `Cache-Control` header, so browsers could silently serve an old cached copy after a deploy. Added `Cache-Control: no-cache` to the HTML response so future updates show up on a normal refresh.
- [x] **Driver application & approval flow built (2026-07-27)** — public "Drive For Us" page (nav + mobile nav + footer links) collecting name/email/phone/vehicle info/license & insurance/experience & availability/notes → `POST /api/driver-applications` (new table, rate-limited, rejects duplicate emails). New admin "Driver Applications" review panel (pending count badge, full applicant details, Approve/Reject). Approving auto-generates a driver login; **emails it if SMTP is configured, otherwise shows the temp password in the admin panel to relay manually** (SMTP isn't set up on this backend yet — same class of gap as BARIO's Resend issue, but a separate system). Verified full cycle end-to-end: submit → duplicate rejected → admin sees it → approve → real driver account created → logs in successfully with the temp password; separately verified reject removes it from the pending list.

- [x] **VPS reselling (Hetzner) shipped and live, all 4 phases (2026-07-29)** — full plan at `C:\Users\surew\.claude\plans\unified-wishing-salamander.md`. Customers can order a self-managed VPS at `/dashboard/servers` → `/dashboard/servers/new` (Small/Medium/Large, monthly/annual/2yr/3yr billing, optional automatic-backups add-on, SSH key or one-time password), fully automated via a Stripe webhook extension (payment → auto-provision on Hetzner, with every account's first order held for manual admin review regardless of risk score; failed payment → suspend after Stripe's retries exhaust; cancellation → deprovision). 6 admin routes (list/approve/reject/retry-provision/force-delete/create) + `vps-reconcile` — this project's first-ever Vercel Cron, every 6h, catching missed webhooks before they silently cost real money. New `/legal/vps-aup` page — **explicitly marked DRAFT, still needs a real lawyer's review before this is legally solid.** Also added monthly/annual billing to the existing site plans (Starter/Business/Agency) while building this. **Still open:** exact VPS/annual pricing is placeholder (~$14.99–$59.99/mo + tiered discounts) pending your sign-off; Hetzner's own ToS re: white-label reselling hasn't been verified yet.

## Not started
- [ ] Wire admin alert emails (new complaint / autonomous action taken) once `EMAIL_FROM` is fixed
- [ ] Decommission old `sunbuiltgroup.com` nginx vhost + cert on the VPS (DNS cutover done — safe to do once you've confirmed the live site looks right)
- [ ] Decommission old `afclogistics.ca` nginx vhost + cert on the VPS (after its migration confirmed stable)
- [ ] Decommission old `rapturemedia.ca` nginx vhost + cert on the VPS (after its migration confirmed stable)

## New requests — need scoping before building

### Admin coupon/discount panel
- Confirmed: `/admin/gift-codes` already exists, but only grants free AI-builder credits via a generic code — no plan upgrades, no $/% discounts, no targeting a specific customer's email, no CRM integration.
- [ ] Extend it (or build alongside it) to: generate a code/discount tied to one specific client's email, admin can upgrade/downgrade or discount any user's plan ($ or %) on demand, pick-and-choose per user rather than a generic redeemable code.

### Mobile-accessible admin dev panel
- You want a bario.ca admin panel where you can edit code with Claude the way we're doing right now, from your phone.
- [x] **First attempt tried and abandoned (2026-08-02): `code.bario.ca`, a code-server (VS Code in the browser) instance on the main VPS, was built for this — but real usage feedback was that it's hard to keep up with / navigate on a phone.** Decommissioned (nginx vhost removed, `code-server@root.service` stopped+disabled) along with a batch of other dead infrastructure discovered on the same VPS during cleanup (see the 2026-08-02 VPS cleanup entry in Done, below).
- [x] **Shipped 2026-08-05, correctly shaped as chat + a link, not a code editor.** You already have this via the Victoria App (`bario.ca/victoria-app`, log in as `uniquegroup.org@gmail.com` — a *different* account than `admin@bario.ca`, which was the actual cause of an earlier "app isn't working" report, not a real bug). Ask Victoria for a coding task in chat (`queue_coding_task`) — it queues into `coding_task_requests` and an hourly "Victoria Coding Dispatcher" routine (real Claude Code cloud session, created via the remote-trigger API, polls `/api/admin/victoria/coding-tasks`) does the actual work, commits it, and narrates progress back into the same chat thread via the existing `/api/admin/victoria/narrate` bridge. **Known limitation, told to the user, not hidden:** up to ~1hr latency before a queued task even starts (routines have a hard 1-hour-minimum interval — there's no instant on-demand trigger available to a plain backend route, only to an active Claude Code session), and the routine commits but does not auto-deploy (this project deploys via `vercel --prod` from local disk, not git push — see `CLAUDE.md`). Also added `dispatch_business_task` (non-coding work, routed through the already-built-but-previously-unwired `lib/agentAgency` Router→Specialist→Critic→Delivery harness) as a second Victoria tool, runs synchronously in the ~1-2min range. Both tools are Victoria-App-only (session-gated to Sherwin) — deliberately not added to the phone-call/SMS Victoria channel, since caller-ID/SMS sender can't be authenticated as strongly and triggering real code changes is a bigger blast radius than her existing call/text/draft tools.

### X-Drive — mostly done now (2026-08-02 status check)
- [x] Standalone installable webapp — **confirmed already satisfied**: `media-manifest.json` is a real, complete PWA manifest (proper icons, `display: standalone`, scoped to `/media`) — no separate app needed.
- [x] **Correction — client-side/end-to-end encryption was already fully built**, this line in the list was stale (the TODO note above saying it "needs a design conversation before building" was written before it shipped and never removed). Real MEK model in `lib/e2eCrypto.ts`: client-generated key, wrapped by both a passphrase-derived key and a recovery-code-derived key, server never sees plaintext of either. Live in `MediaLibrary.tsx` today (enable/unlock/recovery flows all working).
- [x] **Full file management shipped 2026-08-02** — Rename, Move (between folders), and Duplicate/Copy added (`PATCH`/`POST` on `/api/media/[id]`), alongside the existing Upload/Delete. Copy duplicates the DB row pointing at the same underlying blob (no re-upload) but still counts against storage quota, same convention as Google Drive's "Make a copy" — verified live: uploaded a real test file, renamed it, moved it into a folder, duplicated it (quota correctly doubled), deleted both, all via the real deployed API.

### Auto-download for photos/videos (Amazon Photos / Google Drive / OneDrive style)
- Clarified scope: client opts in to auto-upload — when a new photo/video appears on their device, it automatically uploads/backs up into their X-Drive, same UX as Amazon Photos/Google Photos auto-backup.
- [ ] Not started — this is the one real piece of X-Drive still open. Auto-backup-on-device-change implies a companion mobile app or browser extension with device file-system access — needs scoping on what platform(s) first. (The existing folder-watch auto-upload in `MediaLibrary.tsx` via the File System Access API is foreground-tab-only on Chrome/Edge — not true background device sync, a real native/extension capability gap, not a bug.)

### Customer-facing "hPanel"-style dashboard
- [ ] You want a polished, Hostinger-hPanel-style dashboard inside customer accounts so they can find their services/products more easily, doubling as an upsell surface. Not started — needs a mockup/scope conversation before building.

### WordPress Migration & DNS Onboarding Workflow (full spec provided 2026-07-27)
Goal: end-to-end pipeline for WordPress users moving to BARIO — automate content ingestion, and make DNS/nameserver handoff painless, replacing the current fully-manual process used for [[jasmine_blessandseemusic_migration]].

**Phase 1 shipped 2026-07-29, but NOT as originally spec'd — pivoted from a WordPress plugin to a URL crawler, deliberately:**
- The original plan (WP plugin reads the DB, serializes posts/pages to JSON, BARIO re-renders them) doesn't actually work in general — a page's real visual output depends on its specific theme/page-builder/shortcodes, which BARIO can't reimplement for arbitrary WP sites. The proven approach (same one used manually for [[jasmine_blessandseemusic_migration]]) is exporting already-rendered HTML, not reconstructing pages from data.
- Built instead: `POST /api/sites/migrate` — user pastes their live site's URL (no WP admin access, no plugin install needed), BARIO crawls same-origin pages (capped at 25), rewrites internal links to BARIO's page-slug convention, re-hosts `<img>` assets to BARIO's own storage (Vercel Blob), and writes the result into `site_pages` (the multi-page raw-HTML hosting built earlier this project — so multi-page WP import already works, the "reality check" blocker noted below is resolved). Auto-assigns a subdomain and publishes immediately.
- Works for ANY platform (WordPress, Wix, Squarespace, hand-built), not just WordPress — much less friction for non-technical users than installing/configuring a plugin, and matches the original ask's own "or even other types of websites" wording.
- Gated to paid-tier accounts (checked before URL validation). SSRF-guarded (rejects private/loopback/cloud-metadata IPs before crawling).
- **Known v1 limitation, surfaced to the user in the result rather than hidden:** stylesheets/scripts are resolved to absolute URLs but not re-hosted, so visual styling still depends on the original site staying online — only images are truly independent so far. A near-term follow-up would extend re-hosting to CSS/JS/fonts for full independence.
- Live-tested end-to-end against a real BARIO client site (blessandseemusic.com, 5 pages, 13 images) — all pages crawled correctly, live within ~7 seconds.
- UI: `MigrateSitePanel` on `/dashboard/websites` — locked/upgrade-prompt card for free tier, working form + live result summary for paid tier.

**Phase 2 — Guided nameserver-delegation onboarding:**
- Interactive wizard shown after a migration payload arrives, or when a Business/Agency-plan site is created.
- WHOIS-based registrar detection → tailored step-by-step visual instructions per registrar (GoDaddy, Namecheap, Cloudflare, etc.).
- Guide user to switch nameservers to BARIO's managed ones (once), rather than manual A/CNAME records.
- Background propagation checker; once propagated, auto-provision SSL and manage all subsequent subdomains/routing — no more touching the old host.
- Note: this is a different DNS model than what's built today (BARIO currently auto-provisions a *Cloudflare* zone per custom domain and asks for A/CNAME records or Cloudflare nameservers specifically — this phase describes BARIO having its *own* managed nameservers customers delegate to directly, a bigger architectural shift).

**Phase 3 — Registrar OAuth (future growth, not near-term):**
- Direct OAuth integrations with major registrars (Cloudflare, GoDaddy, Namecheap) so nameserver delegation is one click ("Connect with GoDaddy") instead of manual copying, no credential sharing.

- [ ] Not started — this is a multi-week feature, not a quick add. Recommend starting a dedicated planning session for Phase 1 alone once the multi-page hosting question above is resolved, rather than scoping all 3 phases at once.

- [x] **Separate Twenty CRM logins for AFC Logistics and Sunbuilt Group — DONE 2026-07-30, fully verified live.** Both are now independent single-tenant Twenty CRM instances (own Docker stack, own Postgres DB, own subdomain, own login) — no shared workspace, no shared account. **Real root cause finally found and fixed:** the original stuck signup (user created but no workspace, "Workspace not found" on every login attempt) was caused by two compounding issues: (1) the stack was running `twentycrm/twenty:latest`, a bleeding-edge/unstable tag whose compiled `AuthResolver` (all of `signIn`/`signUp`/`checkUserExists`/etc.) was entirely missing from its own served GraphQL schema — confirmed by direct GraphQL calls returning "Cannot query field X on type Mutation" for every auth field, on BOTH `/graphql` and initially-assumed endpoints; (2) the frontend actually calls `/metadata` for all auth operations, not `/graphql` — a wrong assumption from earlier in this session that wasted time debugging the wrong endpoint. **Fix:** pinned both stacks to the latest real stable release, `twentycrm/twenty:v2.25.1` (not `:latest`), wiped the DB volume for a clean schema-matched init, then drove signup via the correct flow: `signUp` (email+password, on `/metadata`) → `signUpInNewWorkspace` (Bearer token from signUp, creates the actual workspace) → real `workspace-activation` step (this takes ~30-60s, genuinely creating the DB schema/data model — this is almost certainly what caused the original stuck state when earlier scripts timed out after only 30-40s, mistaking real provisioning time for a hang) → `create/profile` → `invite-team` (skippable) → real dashboard at `/objects/companies`. Verified end-to-end via both direct GraphQL calls and full real-browser (Playwright) login for both accounts.
  - **AFC Logistics**: `https://afc.crm.bario.ca` — login `admin@afclogistics.ca` / `AfcLogistics2026!Secure`. Stack at `/root/afc-crm-stack` on the main VPS (port 3011).
  - **Sunbuilt Group**: `https://sunbuilt.crm.bario.ca` — login `admin@sunbuiltgroup.com` / `SunbuiltGroup2026!Secure`. Stack at `/root/sunbuilt-crm-stack` on the main VPS (port 3012), DNS record added via Cloudflare API, TLS cert issued via `certbot certonly --webroot` (NOT `--nginx`) to avoid a repeat of the cert-hijacking gotcha below.
  - **Certbot gotcha, worth remembering for any future subdomain under `*.crm.bario.ca`:** running `certbot --nginx -d <new>.crm.bario.ca` will match and silently rewrite `crm.bario.ca`'s OWN nginx server block if that block's `server_name` includes a wildcard like `*.crm.bario.ca` — this actually happened once already this session and broke `crm.bario.ca`'s live cert until caught and fixed via `sed`. From now on, always issue new certs under this domain via `certbot certonly --webroot -w /var/www/certbot -d <new-subdomain>` (temporary HTTP-only vhost for the ACME challenge, then hand-write the final HTTPS server block with explicit `ssl_certificate` paths) — never `--nginx` here.
  - **Still open, not done:** no AI-driven lead-gen/automated emails wired to either CRM yet (see the n8n item above — much closer now that both CRMs genuinely work), no per-client documentation/training on how to use Twenty CRM day-to-day (the user asked for "instruction on how to use it" — not yet written).

## Deferred / bigger ideas (not scheduled)
- [ ] **Email reselling — customer-facing signup shipped 2026-08-02** (see Done above) — `/dashboard/email`, real mailbox+DNS auto-provisioning on Bario's own Mailcow (`reseller.bario.ca`). **Still genuinely open**: no dedicated billing tier (bundled into existing paid plans for now), no white-labeling of the SOGo webmail UI itself, and no branded entry-point URL (`mail.bario.ca` reverse-proxied to SOGo — currently only reachable at `reseller.bario.ca/SOGo`).
- [ ] **Domain registration/reseller — real infrastructure built and verified 2026-08-02, against Namecheap's sandbox (fake money).** Evaluated FOSSBilling (AGPL, "pre-production," separate PHP portal — rejected, would fragment the unified-dashboard experience) and DomainMOD (turned out to be a domain/SSL *expiration tracker*, not a reseller tool at all — different problem). Built instead: a small proxy service (`registrar-proxy`, PM2-managed) on the main VPS at `https://registrar.bario.ca` (real DNS+cert), since Namecheap's API requires a whitelisted calling IP that Vercel serverless functions don't have. `lib/registrar.ts` + `/api/domains/search` talk to it. **Verified live, real API calls**: domain availability search (correctly flagged `google.com` as taken), and a full domain *registration* (`bario-live-test-9012.com`, real sandbox order ID `3639052`, fake charge `$14.18`) — both proven end-to-end through the actual deployed app, not just the proxy directly.
  **Update, same day — dashboard UI shipped and live-verified.** New "Domains" sidebar item → `/dashboard/domains` (`components/DomainRegistration.tsx`). Real search (shows availability + actual per-TLD pricing, e.g. `$14.18 USD/yr` for `.com`), a registrant-details form, and order history — clearly labeled "🧪 Sandbox mode" so nobody mistakes it for live. Verified with a real Playwright browser run through the actual production deployment: searched a fresh domain, registered it, confirmed `status: "registered"` with a real charge amount recorded in `domain_orders`. `/api/domains/register` (POST to register + record the order, GET for order history) is paid-plan-gated like custom domains/email.
  **Update, same day — real Stripe-gated checkout + auto-connect built, then paused mid-verification.** Domain registration now only happens inside the Stripe webhook after payment is confirmed (never on the initial request) — see `bario_domain_reseller` memory for the full architecture. Added auto-connect (Namecheap `setCustom` nameservers → Cloudflare zone → Vercel, no manual step) and an optional organization-name field for business registrants.
  **Discovered Stripe is in LIVE mode** — stopped before entering real card details in testing. Confirmed safely: checkout session creates correctly with the right marked-up price (`$19.99` for a `.com`). User asked to test-buy `autodetailing.com` specifically — checked via real RDAP, **not available** (registered since 1997, active). Also found a real bug: BARIO's own public domain checker (`/api/domains/check`) wrongly returned `"unknown"` for this domain instead of "taken" — not yet fixed.
  **Paused here** — waiting on a decision: pay for real with a different domain to fully verify the webhook chain, get a Stripe test-mode key, or trust the logic and move on. Session moved to Miko's capabilities before deciding. Don't re-verify the sandbox search/registration pieces again, those are already confirmed.
  On the side: found and cleaned up a whole layer of dead/undocumented VPS infrastructure while scoping where to put this proxy — see the 2026-08-02 VPS cleanup entry in Done.
- [x] **Miko voice AI agent — shipped and live-verified 2026-08-02.** User clarified this meant a real AI voice agent (Miko itself talking), not just triggering a human-to-human call. Built on Twilio's real ConversationRelay product (official Anthropic/Claude integration exists) + a new WebSocket service on the main VPS (`miko-voice`, `wss://miko-voice.bario.ca`) + a dedicated phone number (`+18254650880`, deliberately separate from AFC/Sunbuilt/Unique's real lines). Verified live with a real WebSocket test simulating Twilio's exact message sequence — Miko replied correctly and honestly declined to invent details it didn't know. **Still needs a real phone call to fully confirm** (only the WebSocket layer was tested, not actual voice audio) — call `+18254650880` to test. Not yet built: CRM lookup/function-calling mid-call, outbound calling, interruption handling, token streaming.
- [ ] **Miko social media content/engagement automation (raised 2026-08-02)** — drafting/scheduling posts + replying to DMs/comments to grow followers organically. Not started this session. Real blocker: `lib/marketing/*` already has posting code but needs real OAuth credentials from each platform (Twitter/Meta/LinkedIn) and a redesign to support per-client accounts (it's currently one shared BARIO-owned connection). Needs the user to create developer apps on each platform before this can be built.
- [ ] **CRM-Miko's other capabilities (email sending, lead sourcing)** — separate from voice, still needs Twenty CRM's `create-twenty-app` custom-tool mechanism, its own dev/deploy pipeline outside this repo. Not started.
- [ ] Unified admin panel to monitor all ecosystem DBs/services (BARIO, CRM, n8n) for errors — failsafe = alert + revert only, never autonomous fixes
- [ ] FLO rebrand of Twenty CRM (paused by you 2026-07-24)
We need to add an agent or Ai just like the crm.neolinq.pro cause its not able to do anything right now and i want sunbuiltgroup and afclogistics.ca to have some leads and send out emails today and get some responses. 
I want to also add the bario.ca marketing page to the social media accounts so i can start sending out some ads.
Install Bolt https://github.com/stackblitz-labs/bolt.diy.git
we  should offer an Ai assitant service for clients on top of reselling them the phone numbers i think its beneficial and we can design it to their preference and maybe we can show clients what it can do for their business and how its very beneficial . and we can custome build it for them it will be their assistant or receptionist it can be connected to their crm or email and send sms or make phone calls or emails  but we would need to setup a price list for them . 
remind me what else we need for spott.ca so it can be fully completed 
we need to creatae a database for all of the businesses and services they are getting and using and integrate the same service so we can sell it to other companies and customers we can also explain the pros of having our services 
